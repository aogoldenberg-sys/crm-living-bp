name = "crm-compute"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat_v2"]

# Cron: каждые 6 часов — пересчёт планфакта и прогноза.
[triggers]
crons = ["0 */6 * * *"]

# Секреты:
#   wrangler secret put FIREBASE_SERVICE_ACCOUNT_JSON
